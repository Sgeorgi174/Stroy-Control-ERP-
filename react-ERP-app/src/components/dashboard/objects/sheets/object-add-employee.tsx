import type { Object } from "@/types/object";

type ObjectAddEmployeeProps = {
  object: Object;
};

export default function ObjectAddEmployee({ object }: ObjectAddEmployeeProps) {
  return <div>object-add-employee</div>;
}
